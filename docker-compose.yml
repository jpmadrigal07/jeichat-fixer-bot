# Optional. Coolify should use the Dockerfile as a single Application (no domain).
# Use this file only if you create a Coolify "Docker Compose" resource instead.
services:
  fixer:
    build: .
    restart: unless-stopped
